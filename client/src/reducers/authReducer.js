export const authReducer = (state, action) => {
  const { type, payload } = action;

  switch (type) {
    case "Set Auth":
      return {
        ...state,
      };

    case "Set Auth":
      return {
        ...state,
      };

    default:
  }
};
